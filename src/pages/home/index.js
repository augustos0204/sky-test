import React from "react";
import Header from "../../components/header";

const HomePage = () => {
    return (
        <container>
            <Header/>
        </container>
    );
}

export default HomePage;